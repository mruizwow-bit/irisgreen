name: Tanda · fuera el estado público

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preparar:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Preparación temporal: el workflow se reactivará cuando los parches queden escritos en los archivos."
