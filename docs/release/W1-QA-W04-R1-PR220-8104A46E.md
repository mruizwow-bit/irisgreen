# W1-QA-W04-R1 · Veredicto independiente PR #220

**Autorización:** DEC-025 / DEC-020  
**Candidato:** PR #220  
**Base:** `e48b51814afed825a92e83a2f6e51ee8a1c85e45`  
**HEAD:** `8104a46e48d5112286d55532bea8c082f5c42049`  
**Tree:** `9261cf46695d9846828944de83660d805b879bf5`  
**Run exact-head autoritativo:** `35694033573`  
**Veredicto:** `W04_R1_QA_PASS`  
**Estado posterior:** `QA_PASS_WAIT_INTEGRATION`

## Identidad y scope

Compare exacto base→HEAD:

```text
ahead_by   1
behind_by  0
merge-base e48b51814afed825a92e83a2f6e51ee8a1c85e45
changed files 1
```

Único archivo:
`.github/workflows/comprobar-publicacion.yml`

Diff autorizado:
```text
ANTES    999 / 982 / 3 / 14 / 995
DESPUÉS 1003 / 986 / 4 / 13 / 999
```

Más comentario justificativo. No hay cambios en sitemap, canonical, robots, noindex real, hreflang, páginas, rutas, assets, build_site.py, W01, W03, Sabik ni Netlify.

`git diff --check`: PASS.

El patch único fue inspeccionado y no contiene conflict markers.

## Build exacto

QA hizo checkout directo del HEAD candidato, no del merge-ref.

```text
OS      Ubuntu 24.04.5 LTS
kernel  Linux 6.17.0-1022-azure x86_64
Python  3.12.3
Node    v22.23.2
command python3 scripts/build_site.py
exit    0
```

Inventario limpio inmediatamente después del build:

```text
base dist files       1619
candidate dist files  1619
base bytes            439959506
candidate bytes       439959506
added                  0
removed                0
changed                0
```

Los manifests SHA-256 de todos los archivos de `dist` de base y candidato son idénticos. W04-R1 no altera publicación real.

## Contrato de publicación

Resultado sobre el dist exacto:

```json
{
  "html": 1003,
  "index,follow": 986,
  "noindex,follow": 4,
  "otro_o_ninguno": 13,
  "sitemap_urls": 999
}
```

Contrato nuevo: PASS.

Prueba negativa sobre el mismo dist:

```text
old expected 999 / 982 / 3 / 14 / 995
old_contract_would_pass = false
```

El gate sigue siendo estricto; no se relajó para aceptar ambos estados.

`audit_sin_estados_publicos.py`: PASS.

## SEO / indexación

```text
html_pages                1003
pages_with_canonical      1002
pages_with_hreflang        952
reciprocal_language_links  952
sitemap_urls                999
passed                     true
failures                      0
```

Advertencias documentadas:
- `/404.html`: canonical ausente esperado/permitido;
- bridge legacy `/es/tarjetas-iris/` comparte canonical con `/es/recursos/tarjeta-iris/`.

El bridge conserva `noindex,follow` y canonical a la ruta canónica.

No se detectan URLs sitemap sin artefacto, noindex indebido en sitemap ni regresión de hreflang/canonical.

## Workflow nativo del PR

La ejecución nativa de `Comprobación previa de publicación` usó merge sintético:

```text
93bd6eec673d667ee9496075d0ed0a33f5bd00a6
```

Su tree es exactamente:

```text
9261cf46695d9846828944de83660d805b879bf5
```

idéntico al tree del HEAD candidato.

El workflow nativo de publicación: SUCCESS.  
`Comprobar SEO e idiomas`: SUCCESS.

El veredicto QA se basa, no obstante, en el run exact-head independiente `35694033573`.

## Rojos globales

### Storage / privacidad

El candidato no modifica ningún archivo storage/privacy.

QA ejecutó el auditor sobre base y candidato exactos:

```text
base errors       9
candidate errors  9
identical errors  true
```

Clasificación:

```text
PREEXISTING_OUT_OF_SCOPE
```

W04 no declara storage resuelto. La resolución sigue perteneciendo a W03 PR #215, HEAD `6dfecaf24a8a0f2de850eeafc12d41d8b365f2fb`, pendiente de integración.

### Contraste

El workflow `Auditar WCAG contraste con gradientes` en #220 registra:

```text
unique_signatures          115
best_case_white_failures     0
pixel_probe_signatures      17
pixel_probe_failures         4
```

El diff W04 no toca CSS/HTML/producto y el dist base/candidato es byte a byte idéntico.

W02 ya clasificó las 4 firmas como `TRUE_WCAG_CONTRAST_DEFECT`.

Clasificación W04:

```text
PREEXISTING_OUT_OF_SCOPE
```

No son falsos positivos y siguen pendientes de corrección.

Por tanto PR #220 puede pasar el gate de scope W04, pero **no es release-ready global** hasta integrar/corregir W03 y contraste.

## IG-SABIK-DELIVERY-MANIFEST/1.0

```json
{
  "schema": "IG-SABIK-DELIVERY-MANIFEST/1.0",
  "Work_ID": "W1-QA-W04-R1-PR220",
  "Decision_ID": ["DEC-025", "DEC-020"],
  "base": "e48b51814afed825a92e83a2f6e51ee8a1c85e45",
  "head": "8104a46e48d5112286d55532bea8c082f5c42049",
  "tree": "9261cf46695d9846828944de83660d805b879bf5",
  "QA_Tested_SHA": "8104a46e48d5112286d55532bea8c082f5c42049",
  "Build_Tested_SHA": "8104a46e48d5112286d55532bea8c082f5c42049",
  "run": 35694033573,
  "artifact_id": 10680020330,
  "artifact_digest": "sha256:06c7d447252bb970de3726250cf460d4be432220420accbc289671fb90180fd9",
  "build_command": "python3 scripts/build_site.py",
  "build_exit_code": 0,
  "dist_files": 1619,
  "dist_bytes": 439959506,
  "manual_pending": [],
  "publication": "1003/986/4/13/999 PASS",
  "rollback": "revert 8104a46e48d5112286d55532bea8c082f5c42049 to restore prior workflow contract; no product/data migration"
}
```

## Limitaciones

- W04-R1 certifica el contrato de publicación y su scope, no la resolución global de storage o contraste.
- El run QA exact-head no despliega.
- El corpus público real sigue igual que en la base: el cambio es exclusivamente de expected congelado.
- Un helper no vinculante del runner exact-head intentó usar un rev-range con `git grep` y emitió un `fatal` benigno bajo una condición negada. La comprobación vinculante `git diff --check` pasó y el patch único fue inspeccionado independientemente sin conflict markers. Build, contrato, SEO, comparación de `dist`, storage y manifiesto no dependen de ese helper.

## Estado remoto

PR #220 sigue:

```text
OPEN
DRAFT
merged=false
HEAD=8104a46e48d5112286d55532bea8c082f5c42049
```

No se modificó el candidato.

## Veredicto

```text
W04_R1_QA_PASS
QA_PASS_WAIT_INTEGRATION
```

**NO MERGE. NO DEPLOY. DETENERSE.**
